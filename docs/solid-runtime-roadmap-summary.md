# Solid Runtime Roadmap Summary

The expanded roadmap docs add planning coverage for:

- the Go/Rust Solid runtime implementation sequence;
- CSS-compatible authn/authz progression from shadow mode to enforcement gates;
- `did:solid` as a project-defined DID method extension;
- Gzip and Zstd support through compatibility-preserving negotiation.

The authoritative details live in:

- `docs/solid-runtime-roadmap-index.md`;
- `docs/solid-runtime-phase-roadmap.md`;
- `docs/did-solid-method.md`;
- `docs/compression-compatibility.md`.
