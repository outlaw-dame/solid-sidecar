# Roadmap PR Notes

This branch is documentation-only.

It adds implementation planning for:

- Go/Rust Solid runtime phases;
- CSS-compatible shadow-to-enforcement progression;
- Gzip and Zstd compression compatibility;
- project-defined `did:solid` support.

No runtime behavior changes are included.
