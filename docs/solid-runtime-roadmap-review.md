# Roadmap Review Note

Review focus:

- whether the phase ordering matches the current missing implementation work;
- whether Gzip/Zstd compatibility rules are conservative enough;
- whether `did:solid` is clearly framed as identity binding, not authorization bypass.
